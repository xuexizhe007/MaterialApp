name: material_app
description: A new Flutter project.
publish_to: 'none'
version: 1.3.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  pdf: ^3.10.4
  printing: ^5.11.0
  # 新增：扫码库
  mobile_scanner: ^5.0.0
  # 新增：权限处理
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    # 请确保您的字体文件名是 FangSong.ttf 并且放在 assets/fonts/ 目录下
    - assets/fonts/FangSong.ttf
